name: Hello GitHub Actions

on:
  push:

jobs:
  hello:
    runs-on: ubuntu-latest

    steps:
      - run: echo "Hello DevOps"

      - run: pwd

      - run: whmi

      - run: uname -a

      - run: ls -la
